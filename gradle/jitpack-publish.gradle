afterEvaluate {
    publishing {
        publications {
            release(MavenPublication) {
                from components.release
                groupId = "com.github.AlvaroMonserrat.LugalCompose"
                artifactId = "lugal"
            }
        }
    }
}